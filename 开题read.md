# 基于 YOLO11 的安全帽佩戴检测系统设计

## ——本科毕业设计（论文）开题报告

| 项目 | 内容 |
|---|---|
| 学生姓名 | 蔡秉昊 |
| 学号 | （待填写） |
| 专业 / 班级 | （待填写） |
| 指导教师 | （待填写） |
| 开题日期 | 2026 年 9 月 |

---

## 摘要

在建筑施工、电力检修、生产制造等作业现场，安全帽是保护作业人员头部免受伤害最基本、也最关键的防护装备。目前对安全帽佩戴情况的检查主要依靠人工巡检，存在覆盖不连续、耗费人力、难以追溯等问题。本课题拟设计并实现一套基于 YOLO11 的安全帽佩戴检测系统，利用现场摄像头拍摄的图像和视频自动识别人员是否佩戴安全帽，为现场安全管理提供技术辅助。

本课题以公开数据集 SHWD（Safety Helmet Wearing Dataset）作为数据来源，从原始数据的整理与校验开始，逐步完成模型训练、对比实验、性能评估与系统实现，是一个从零开始、逐步搭建的完整过程。系统的核心难点在于：作业现场的人员在画面中往往只占很小的范围，安全帽在图像中可能只有十几个甚至几个像素，很容易被漏检。因此，本课题把**微小目标的漏检问题**作为主要研究方向，围绕输入分辨率、模型容量、数据分布、检测头结构四个方面设计了一系列对照实验，逐项考察每种改进方法能带来多少精度提升、又要付出多少计算代价，从而确定适合实际使用的模型方案。

目前课题已经完成了数据集的整理与校验、基线模型的训练与评估、多组对照实验以及检测阈值的确定，并编写了相应的程序代码。本报告将说明课题的研究背景、研究现状、研究内容、技术路线以及下一步的工作安排。

**关键词：** 安全帽检测；YOLO11；目标检测；小目标；深度学习

---

## 一、选题背景与意义

### 1.1 研究背景

头部伤害是建筑、电力、制造等高风险行业中最主要的伤亡类型之一。在高处作业、交叉施工等场景下，一旦发生物体打击或人员坠落，安全帽是成本最低、防护效果最直接的个体防护装备。美国职业安全与健康管理局（OSHA）在 1910.135 条款中明确规定，雇主必须确保相关员工佩戴头部防护装备[^osha]。

然而，法规约束的是雇主的管理责任，并不是技术方案的性能指标。OSHA 并没有规定"检测系统召回率必须达到多少"，这意味着自动检测**不能替代现场的安全管理责任**，只能作为一种辅助手段，帮助管理人员更早地发现问题[^osha][^nist]。

目前施工现场对安全帽佩戴情况的检查，主要依靠安全员巡检和班组长的目视观察，存在三个明显的不足：

1. **覆盖不连续**——巡检是抽查性质的，很难做到对全部作业面和全部时段持续覆盖；
2. **耗费人力且难以追溯**——依赖人工投入，且缺少结构化的记录，事后统计和追责都比较困难；
3. **难以实时干预**——即使发现违规行为，往往也滞后于风险发生的时刻。

因此，如果能够利用现场已有的监控摄像头，自动、连续地识别未佩戴安全帽的人员并及时提醒，就能把"事后追责"前移为"事中预警"，这正是本课题希望解决的问题。

### 1.2 本课题的难点

安全帽检测看起来只是"把安全帽框出来"，但在实际做的时候，我遇到了几个具体的困难：

**（1）目标太小。** 作业现场画面中，人员往往只占很小的区域，安全帽在图像上可能只有十几像素甚至更小。网络经过多次下采样后，这些小目标的特征很容易在中间层就消失掉，导致漏检。这是本课题最主要的技术难点。

**（2）两类目标外观非常接近。** 数据集中的两类目标——"戴安全帽的头部"和"未戴安全帽的头部"——在低分辨率下的差别，仅仅体现在头顶那一小块区域的像素上，很容易发生类别混淆。

**（3）对速度有要求。** 安全提醒需要在秒级内完成，系统必须能够实时处理视频画面，不能只追求精度而不顾速度。

**（4）数据集与真实现场存在差距。** 已有研究指出，SHWD 数据集中缺少复杂环境下的极小目标，且未戴帽的图像有相当一部分来自非施工场景，因此它并不能完全代表真实的施工现场[^shwd-limits]。这个问题需要在课题中正视。

**（5）"检测到什么程度才算够用"并没有现成答案。** 我查阅资料后发现，并不存在权威统一的召回率及格线。NIST 人工智能风险管理框架与 ISO/IEC 23894 都强调，风险容忍度要根据具体的应用场景来确定[^nist][^iso]；视频分析类系统更适合按"事件"而不是按"单帧"来评价[^bosch]。也就是说，"系统每个月最多能漏掉几起违规"这样的问题，需要结合使用场景来回答。

### 1.3 研究意义

**从学习与锻炼的角度看**，本课题覆盖了深度学习目标检测从数据准备、模型训练、实验设计、性能评估到系统实现的完整流程。通过亲手完成这一整套工作，我可以把课堂上学到的卷积神经网络、目标检测等知识真正用到实际问题上，尤其是通过设计对照实验来验证改进方法是否有效，这对培养工程思维和科研素养都很有帮助。

**从实际应用的角度看**，本课题最终交付的不仅是一个训练好的模型文件，而是一套可以运行的检测程序，能够在视频上完成实时检测与追踪。虽然它还需要经过现场数据验证才能真正投入使用，但可以为后续的现场部署提供一个可用的基础。

**从方法探索的角度看**，小目标检测的改进方法很多（数据增强、损失函数、标签分配、高分辨率检测头等），但这些方法在**计算资源有限、又对速度有要求**的实际系统中，哪一种更值得投入，公开资料中缺少系统的横向对比。本课题希望通过在同一基线上做对照实验，给出一个可以参考的结论，同时把无效的尝试也记录下来，避免其他人重复走弯路。

---

## 二、国内外研究现状

### 2.1 目标检测技术的发展概况

深度学习目标检测大致有两条主要技术路线。一条是**两阶段检测器**（以 Faster R-CNN 为代表），先产生候选区域再对每个区域分类，精度较高但速度偏慢；另一条是**单阶段检测器**（以 YOLO、SSD、RetinaNet 为代表），直接在特征图上预测目标的位置和类别，在速度和精度之间取得了更好的平衡。近年来还出现了基于 Transformer 的检测器（如 DETR 系列），采用端到端的方式输出检测结果，简化了后处理步骤，但训练成本相对较高。

考虑到本课题需要在现场视频上实时运行，对速度有明确要求，因此选择在工业界应用最广、部署工具链最成熟的 YOLO 系列作为基础，具体采用 Ultralytics 的 YOLO11[^ultralytics]。

### 2.2 安全帽检测的研究现状

**在检测精度方面**，近年来已有不少针对安全帽检测的改进工作。一项基于改进 YOLOv10 的研究提出了小波动态增强方法，在 SHWD 数据集上取得整体精确率 92.9%、召回率 87.6%[^yolov10]；一项面向电力作业防护用品检测的研究（MRC-DETR）报告安全帽类别的精确率 0.966、召回率 0.948[^mrcdetr]。公开的工程实践中，`yolo11-hard-hat-detection` 项目报告精确率 0.925、召回率 0.897、mAP50 为 0.943，不过该项目自己也说明它是一个"可复现的实验流程"，而不是用于生产的安全监控系统。

**在数据集方面**，有研究明确指出 SHWD 缺少复杂环境下的极小目标，未戴帽的图像大量来自非施工场景，因此不能算作标准的现场数据集，无法满足真实生产环境的验证要求[^shwd-limits]。这一点对本课题同样适用——即使模型在 SHWD 上取得较高指标，也仍然需要独立的现场数据进行验证。

**在"达到什么程度才能用"方面**，公开研究大多只报告 mAP、精确率、召回率，很少给出每小时误报次数、违规事件的检出率等指标，因此很难从论文里的数字直接推断出能不能实际部署[^nist][^bosch]。实际工程中，`worksite-safety-monitor` 项目把逐帧的检测结果聚合成持续事件（设定 1.5 秒的宽限窗口和 3 秒的最短持续时间），这说明用于实际系统的最终输出应该是"事件"，而不是孤立的一帧检测框。

### 2.3 小目标检测的研究现状

针对小目标检测这一难点，已有的改进方法大致可以分成三类：

**（1）从数据入手的方法。** Kisantal 等人提出对小目标进行复制增强，把小目标实例复制粘贴到图像中，增加单张图里小目标的数量；但论文也明确指出，这样做是**用大目标的检测质量去换小目标的检测质量**[^kisantal]。Scale Match 方法指出，训练阶段和推理阶段的目标尺度分布如果不匹配，会损害微小目标的特征表达，因此裁剪放大后的尺度需要加以控制[^scale-match]。SNIP 和 SNIPER 提出尺度受控训练，只让处于合适尺度范围内的目标参与梯度回传，并引入背景图像块来控制误检[^snip][^sniper]。还有研究强调，目标级复制必须保留上下文信息，孤立地粘贴目标会产生位置不合理和外观不一致的问题[^context-aug][^traffic-context]。

**（2）从度量方式和标签分配入手的方法。** 归一化高斯瓦瑟斯坦距离（NWD）指出，交并比对微小目标框的少量像素偏移极为敏感，并提出了更适合微小目标的相似度度量方式[^nwd]。RFLA 通过高斯感受野距离和分层标签分配，改善微小目标的正样本分配[^rfla]。此外，在线困难样本挖掘（OHEM）和 Focal Loss 分别从样本选择与损失加权的角度处理困难样本和不平衡问题[^ohem][^focal]。不过这些方法的官方实现多基于 MMDetection 等框架，如果要迁移到 YOLO11 的标签分配器上，需要重写训练损失和分配逻辑，改动量比较大。

**（3）从网络结构入手的方法。** 最直接的做法是增加 P2 高分辨率检测头。QueryDet 进一步分析了小目标检测困难的原因——下采样后特征消失、背景噪声干扰、感受野不匹配等，并提出先用低分辨率粗略定位，再只在可能存在小目标的区域上做高分辨率计算，从而在降低计算量的同时保持小目标精度[^querydet]。

### 2.4 现有研究的不足与本课题的切入点

综合以上现状，我认为存在三点不足：

1. **整体指标已经不错，但小目标漏检是共同的短板。** 已有工作在数据集内的整体指标上已经比较成熟，但对于"小目标为什么会漏检、从哪个方向改进最有效"，缺少系统的对照分析。
2. **改进方法很多，但在实际约束下的性价比排序不清楚。** 大多数研究单独验证某一种方法的有效性，很少在同一个基线和同一套评价标准下，横向比较数据类改进和结构类改进各自要付出多少代价。
3. **论文里的指标和实际能用的标准之间还有距离。** 逐帧的召回率无法直接回答"系统每个月会漏掉几起真实违规"。

因此，本课题希望以一套自己搭建的检测系统为基础，通过**每次只改一个变量的对照实验**，回答"在计算资源和速度都受限的情况下，从哪个方向改进最值得投入"这个问题。

---

## 三、研究目标、内容与关键问题

### 3.1 研究目标

1. 建立一套可靠的数据处理流程：从原始的 SHWD 数据出发，完成数据格式转换，并独立校验转换结果的正确性，保证训练用的是干净、可信的数据。
2. 通过设计对照实验，逐个考察输入分辨率、模型容量、数据分布、检测头结构这四个方面对微小目标检测的影响，并统计每种改进带来的精度收益和计算代价。
3. 在保证能够实时处理视频的前提下，确定最终使用的模型方案，并实现视频中的多目标追踪功能。
4. 把逐帧的检测指标换算成更容易理解的"事件级"评价方式，并说明检测系统在实际安全管理中应该扮演什么角色。

### 3.2 主要研究内容

| 编号 | 研究内容 | 具体说明 |
|---|---|---|
| **内容一** | 数据集的整理与校验 | 数据检查、VOC 格式到 YOLO 格式的转换、转换结果的独立反算校验、训练/验证/测试集的划分 |
| **内容二** | 小目标检测的对照实验 | 分三个阶段共十组实验：主干选型（M4→E4）、数据层面的改进（E5a/E5b）、检测头结构的改进（E6/E7/E8） |
| **内容三** | 检测阈值确定与实时追踪 | 通过单变量扫描确定置信度和 NMS 交并比；实现视频中的实时多目标追踪 |
| **内容四** | 检测系统的实现 | 把训练好的模型封装成可以调用的检测程序，包括图片检测、视频检测、实时追踪和检测服务 |

### 3.3 拟解决的关键问题

**关键问题一：怎样才能在整体指标不下降的前提下，提升微小目标的召回率？**
在尝试中发现，从数据层面入手虽然能提升微小目标的召回率，但同时会带来误检增加、整体指标下降的问题。这让我意识到需要先弄清楚：瓶颈究竟在数据分布上，还是在网络结构对微小目标的感知能力上。

**关键问题二：结构上的改进怎样才能兼顾精度和速度？**
增加高分辨率检测头能明显提升微小目标的召回率，但计算量代价很大。需要设计更轻量的结构改进方案，在能够实时运行的前提下尽量靠近这个精度收益。

**关键问题三：怎样把逐帧的检测指标，换算成能说明问题的评价标准？**
安全相关的应用不能只看离线数据上的召回率。需要建立一个从"允许漏报数量"反推召回率要求的换算思路，并设计以事件为单位的评价指标。

### 3.4 课题特色

> 需要说明的是，本课题的重点**不是提出新的网络结构或损失函数**，而是在 YOLO11 和 SHWD 这两个已有基础之上，完成一套系统的检测方案设计与验证。课题的特色主要体现在做法上：

1. **每次只改一个变量，并且事先定好成功标准。** 每组实验开始之前，先写清楚"达到什么程度算成功"（例如"微小目标召回率提升，且整体 F1 下降不超过 0.002"），再动手跑实验，用数据而不是感觉来判断改进是否有效。
2. **失败的实验也完整记录。** E5a 的重采样实验和 E5b 的裁剪实验都没有达到预期目标，我把它们作为失败记录保留下来。它们说明"这条路走不通"，反而帮助我定位到真正的瓶颈在网络结构上，也为后续实验指明了方向。
3. **不只看精度，同时看计算代价。** 每组实验都同时记录参数量、计算量（GFLOPs）、推理耗时和帧率，把速度作为和精度同等重要的指标来考虑，而不是只追求指标好看。
4. **数据必须可信。** 转换后的数据不自己证明自己，而是用独立的程序重新读取原始标注文件，逐框反算比对，坐标误差必须小于 0.01 像素。遇到无法确认的情况宁可让程序报错停下，也不产出有问题的数据。
5. **从零开始完成整套流程。** 从原始数据的处理，到模型训练、实验对比、系统实现，都由本人独立完成，不是在一个现成系统上做局部修改。

---

## 四、技术路线与系统方案设计

### 4.1 总体技术路线

```text
                    ┌─────────────────────────────────┐
                    │  第一步：数据整理与校验           │
                    │  数据检查 → VOC 转 YOLO          │
                    │  → 独立反算校验 → 划分数据集       │
                    └────────────────┬────────────────┘
                                     ▼
                    ┌─────────────────────────────────┐
                    │  第二步：主干模型对比实验          │
                    │  M4 基线 → E1 训练轮数            │
                    │  → E2 输入分辨率 → E3 模型容量     │
                    │  → E4 组合方案                    │
                    └────────────────┬────────────────┘
                                     ▼
        ┌────────────────────────────┴────────────────────────────┐
        ▼                                                         ▼
┌───────────────────────┐                        ┌───────────────────────┐
│ 第三步：数据层面的改进   │                        │ 第四步：结构层面的改进  │
│ （实验后判定效果不佳）   │                        │ （实验后判定有效）      │
│ E5a 困难样本重采样      │                        │ E6  增加 P2 检测头     │
│ E5b 上下文裁剪放大      │                        │ E7  P2 + 轻量精炼模块  │
└───────────┬───────────┘                        │ E8  去掉 ECA 的对照    │
            │                                    └───────────┬───────────┘
            └────────────────────┬───────────────────────────┘
                                 ▼
                    ┌─────────────────────────────────┐
                    │  第五步：阈值确定与实时化          │
                    │  置信度 / NMS 交并比单变量扫描      │
                    │  视频实时追踪（≥25 FPS）           │
                    └────────────────┬────────────────┘
                                     ▼
                    ┌─────────────────────────────────┐
                    │  第六步：系统实现与总结            │
                    │  检测程序封装 + 现场验证方案       │
                    │  + 论文撰写                       │
                    └─────────────────────────────────┘
```

### 4.2 数据集

本课题使用公开数据集 SHWD（Safety Helmet Wearing Dataset），原始数据为 VOC 格式，包含两个类别：`hat`（佩戴安全帽）和 `person`（未佩戴安全帽）。

| 划分 | 图像数 | 用途 |
|---|---|---|
| 训练集 | 5,457 | 用于模型训练 |
| 验证集 | 607 | 用于选择模型、调整参数和确定阈值，所有实验共用同一套 |
| 测试集 | 1,517 | **仅用于最终的独立评估**，不参与训练、选模、调参和阈值选择 |

**数据转换规则**（由程序强制保证）：

- `hat` 转换为类别 0（helmet），`person` 转换为类别 1（no_helmet）；`dog` 类别直接忽略并记入报告；
- 如果出现其他未预期的类别，程序**在写出数据之前直接报错停止**；
- 没有有效标注目标的图片保留下来，并生成对应的空标签文件；
- 标签坐标反算回像素坐标的误差必须小于 0.01 像素；
- 只有在确认解码后的像素完全一致时，才允许裁剪 JPEG 文件末尾的多余字节，并记录文件校验值和裁剪的字节数。

### 4.3 评价指标

| 层次 | 指标 | 用途 |
|---|---|---|
| 标准评估 | mAP50、mAP50-95、精确率、召回率 | 与公开研究进行横向比较 |
| 固定阈值统计 | 正确检出（TP）/ 漏检（FN）/ 误检（FP）/ F1 | 分析微小目标的检测情况 |
| 尺寸分档 | 微小目标（≤10 像素）与小目标（≤20 像素）分别统计召回率 | 定位问题出在哪一档目标上 |
| 效率指标 | 参数量、GFLOPs、单张推理耗时、帧率 | 计算改进方案的速度代价 |
| 部署指标 | 不同推理后端下的延迟与吞吐 | 说明部署的可行性 |
| **（拟补充）事件级** | 违规事件召回率、每小时误报次数、告警延迟 | 作为实用性的评价标准 |

> **说明：** 本课题同时使用 mAP 和固定阈值统计两套指标是有意为之。mAP 是对所有目标在所有阈值下的积分，容易被大量容易检测的目标"拉高"，从而掩盖微小目标的漏检；而固定阈值统计则能直接回答"在实际使用阈值下，到底漏掉了几个、误报了几个"。

### 4.4 实验方案设计

为了保证结论可靠、每次改进的效果都能说清楚，本课题采用以下做法：

1. **每次只改一个变量。** 每组实验只改变一个方面（训练轮数、输入分辨率、模型容量、数据分布或检测头结构），其他参数全部保持不变。
2. **事先定好判断标准。** 每组实验开始前先写明"达到什么程度算成功"，实验结束后对照标准判断，减少主观因素的影响。
3. **评价标准统一。** 所有实验都在同一套完整的验证集（607 张图片、9,925 个标注框）上评估。同时明确标注各系列实验所用的参数配置（E5 系列以 conf=0.20、IoU=0.50 为基准；E6-E8 系列以 conf=0.25、IoU=0.70 为基准），避免混淆。
4. **测试集严格隔离。** 测试集不参与训练、选模、调参和阈值选择，只在最后评估时使用一次。
5. **失败的实验同样记录。** 没有达到目标的实验也要完整保存配置、权重和报告。

### 4.5 系统总体设计

```text
数据处理部分    SHWD 原始数据（只读）→ 检查 → 格式转换 → 独立校验 → 划分数据集
                    │
模型训练部分    对照实验训练 → 评估（标准指标 + 固定阈值统计 + 尺寸分档）
                    │
检测推理部分    图片检测 / 视频检测 → 多目标追踪 → 实时追踪
                    │
系统实现部分    模型导出 → 推理性能测试 → 检测服务
                    │
测试保障部分    自动化测试，覆盖数据处理、训练、评估、推理等各环节
```

**程序模块划分**（[`src/helmet_safety/`](src/helmet_safety/)）：

| 模块 | 主要功能 |
|---|---|
| [`data/`](src/helmet_safety/data/) | SHWD 数据检查、VOC 转 YOLO、独立反算校验、VOC 文件解析 |
| [`training/`](src/helmet_safety/training/) | 各组实验的训练、评估、阈值扫描、错误样本分析 |
| [`inference/`](src/helmet_safety/inference/) | 基于 OpenCV 的图片/视频检测、分块推理、半精度推理 |
| [`tracking/`](src/helmet_safety/tracking/) | ByteTrack / BoT-SORT 跟踪器封装、画面绘制、实时追踪 |
| [`service/`](src/helmet_safety/service/) | 检测服务接口、模型文件校验、运行状态统计 |
| [`quality/`](src/helmet_safety/quality/) | 依赖版本检查 |

---

## 五、课题已开展的工作与阶段性结果

> 以下结果都是本课题开展过程中实际跑出来并记录在案的。所有原始产物和校验值保存在 [`artifacts/ARTIFACTS_INDEX.md`](artifacts/ARTIFACTS_INDEX.md) 中。

### 5.1 数据集的整理与校验

这是课题的第一步。为了让后续实验建立在可靠的数据上，我编写了一套数据处理程序，主要做了四件事：

- **数据检查**：以只读方式扫描 SHWD 原始数据，统计类别分布和标注情况，不修改任何原始文件；
- **格式转换**：按照 4.2 节的转换规则，把 VOC 格式转换为 YOLO 格式，遇到未预期的情况直接报错停止；
- **独立校验**：校验程序不读取转换程序生成的报告，而是**重新读取原始 XML 文件和转换后的标签文件，逐框比对坐标**，反算误差必须小于 0.01 像素；
- **生成文件管理**：用一个清单文件记录程序生成过的所有文件，需要清理时精确删除，不会误删原始数据。

这一步做完之后，训练和验证使用的数据是经过双重检查的。

### 5.2 主干模型对比实验

从基线模型（YOLO11n，输入 640，训练 50 轮）出发，逐个验证"多训练几轮""提高输入分辨率""换更大的模型"分别能带来多少提升。评估使用标准的 YOLO 验证方式，在完整的 607 张验证集上进行。

| 实验 | 模型 | 输入 | 轮数 | 精确率 | 召回率 | mAP50 | mAP50-95 | 相比基线 | 结论 |
|---|---|---|---|---|---|---|---|---|---|
| **M4** | YOLO11n | 640 | 50 | 0.9289 | 0.8894 | 0.9383 | 0.6038 | — | 基线 |
| **E1** | YOLO11n | 640 | 100 | 0.9336 | 0.8809 | 0.9422 | 0.6105 | +0.67 pp | 多训练 50 轮提升有限，说明 50 轮后已进入平台期 |
| **E2** | YOLO11n | 960 | 50 | 0.9445 | 0.9134 | 0.9590 | 0.6326 | +2.88 pp | 提高输入分辨率效果明显，微小目标召回率从 0.484 提高到 0.578 |
| **E3** | YOLO11s | 640 | 50 | 0.9450 | 0.8950 | 0.9560 | 0.6281 | +2.42 pp | 换用更大的模型有效，但在 640 输入下仍受分辨率限制 |
| **E4** | YOLO11s | 960 | 75 | 0.9491 | 0.9281 | 0.9650 | 0.6430 | +3.92 pp | ✅ **最终选用**，分辨率和模型容量同时提高 |

**这一阶段的结论：** 提高输入分辨率（E2）和换用更大的模型（E3）是两个主要的提升来源，两者结合（E4）效果最好；单纯增加训练轮数（E1）收益有限，不太值得多花时间。模型参数量从 2.59M 增加到 9.41M，说明微小目标确实需要更大的有效像素和更强的特征表达能力。

### 5.3 数据层面的改进尝试

针对 E4 仍然存在的微小目标漏检问题，我从数据分布的角度尝试了两条路线。评估使用固定阈值统计方式，以 E4 在 conf=0.20、NMS IoU=0.50 下的结果作为基准。

| 实验 | 方案 | 微小目标召回率 | 微小目标 F1 | 总体 TP/FN/FP | 总体 F1 | 结果 |
|---|---|---|---|---|---|---|
| E4（基准） | 基准配置 | 0.6172 | 0.6077 | 9420 / 505 / 951 | 0.9283 | 参考基准 |
| 延长训练对照 | 同样轮数的公平对照 | 0.6484 | 0.6014 | 9446 / 479 / 1067 | 0.9244 | 公平对照组 |
| **E5a** | 对微小困难样本定向重采样 | 0.6719 | 0.5931 | 9409 / 516 / 1088 | 0.9215 | ❌ **无效**：微小目标召回率上升，但整体指标下降，未达到事先定的标准 |
| **E5b-B1** | 对微小目标做上下文裁剪放大 | 0.6563 | 0.6109 | 9431 / 494 / 1058 | 0.9240 | ⚠️ 效果有限，整体不如 E4，作为记录保留 |

**关于失败原因的进一步分析（这是本课题的一个重要发现）。** E5a 相比公平对照只多检出了 3 个微小目标，却多出了 11 个误检。我进一步检查训练数据后发现，问题的关键不只是"重复"，而是**重复过于集中**：额外增加的 1,043 个训练位置只覆盖了 151 张图片，其中前 10 张就占了额外抽样的 31%，单张图片最多被重复使用了 74 次。被高频重复的图片里还有标注极其密集的样本（单张图有 152 个真实框）。这说明模型其实是在反复记忆少数几张图的背景和拍摄条件，而不是真正学到了微小目标的多样性，表现上更像是"记住了场景"，而不是"学会了识别"。

**这一阶段的结论：** 从数据层面入手虽然能局部提升微小目标的召回率，但都会伴随着整体误检增加或综合指标下降，没有达到预期目标。这个失败的结果反过来说明——**瓶颈不在数据分布，而在网络结构对微小目标的感知能力**，这为第三阶段的实验指明了方向。

### 5.4 检测头结构的改进

根据上一阶段的结论，我尝试从网络结构上直接增强模型对微小目标的感知能力：在原有检测头的基础上增加一个 P2 高分辨率检测头，并设计了一个轻量特征精炼模块（Lite Feature Refinement, LFR）来补充细节信息。评估使用标准验证方式加固定阈值统计（conf=0.25、NMS IoU=0.70），以 E4 的标准配置作为基准。

| 实验 | 结构 | 参数量 | GFLOPs@960 | 精确率 | 召回率 | mAP50 | mAP50-95 | 固定阈值 F1 | 类别混淆 | 单张耗时 | 帧率 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **E4** | YOLO11s | 9.41M | 48.5 | 0.9491 | 0.9281 | 0.9650 | 0.6430 | 0.9252 | 14 | 12.5 ms | 79.9 |
| **E6** | 增加 P2 检测头 | 9.66M | 76.7 | 0.9434 | 0.9236 | 0.9668 | 0.6445 | 0.9242 | 20 | 21.4 ms | 46.8 |
| **E7** | P2 + 轻量精炼模块 | 9.68M | 78.8 | 0.9532 | 0.9231 | 0.9672 | **0.6479** | **0.9283** | **10** | 23.5 ms | 42.5 |
| **E8** | P2 + 精炼模块（去掉 ECA） | 9.68M | 78.8 | 0.9544 | 0.9228 | 0.9683 | 0.6440 | 0.9257 | 15 | 22.9 ms | 43.7 |

**微小目标（≤10 像素）召回率分档对比：**

| 实验 | 微小目标总体召回率 | 安全帽类微小目标 | 未戴帽类微小目标 |
|---|---|---|---|
| E4 | 0.6016 | 0.2857 | 0.6404 |
| E6 | **0.6484** | 0.3571 | **0.6842** |
| E7 | 0.6172 | 0.2857 | 0.6579 |
| E8 | 0.6172 | **0.4286** | 0.6404 |

**这一阶段的结论：**

- **E6（增加 P2 检测头）**：微小目标召回率提升最明显（+4.69 个百分点），但计算量增加了 58%，推理速度几乎减半，而且类别混淆反而变多（从 14 增加到 20）。**精度收益抵不上速度代价。**
- **E7（P2 + 轻量精炼模块）**：轻量特征精炼模块由深度可分离卷积、ECA 通道注意力和残差连接组成，只用 +0.19% 的参数量，就换回了全部实验中**最高的 mAP50-95（0.6479）和最少的类别混淆（10）**，是精度和速度之间比较理想的平衡点。这个改进是有效的。
- **E8（去掉 ECA 的对照实验）**：去掉 ECA 通道注意力之后，精度回落、类别混淆增多，说明 ECA 在 P2 浅层特征增强中确实起了作用，也说明 E7 的效果来自整个精炼模块，而不是偶然。
- **最终选择**：综合精度和速度考虑，**E4 作为实际使用的模型**，E7 作为后续可以继续优化的方向。

### 5.5 检测阈值的确定

模型定下来之后，我用一组固定的微小目标验证子集，对后处理参数做了单变量扫描，想搞清楚"把置信度阈值调低，能救回多少漏检，代价是多少误检"：

- **置信度扫描**：当置信度从 0.25 降到 0.15 时，微小目标的正确检出从 77 个增加到 83 个（多 6 个），但误检从 250 个增加到 420 个（增加 1.68 倍）。**从 0.15 开始误检明显增多**，这也是后面实时追踪采用分层置信度策略的依据。
- **NMS 交并比扫描**：交并比不大于 0.70 时没有重复框，误检数量稳定；**达到 0.80 之后出现明显的重复框**（0.80 时出现 98 对，涉及 26 张图片）。0.70 是比较合适的取值。
- **组合验证**：在置信度和交并比的四种组合中，固定阈值下总体 F1 最高的是 conf=0.25、IoU=0.50（0.9313）；而 E5 系列实验采用 conf=0.20、IoU=0.50 作为基准，是为了兼顾微小目标。
- **分类别阈值**：在 E6 上筛选出 37 个可行的工作点，按"满足约束后总体 F1 最大"的原则，选出了分类别的阈值（`helmet` = 0.42，`no_helmet` = 0.25），该工作点总体 F1 为 0.9251、未戴帽类别召回率为 0.9399。**这组数据也是后面讨论"实用性标准"的重要依据。**

> **体会：** 阈值不是凭感觉定的，而是扫出来的；降低阈值救回来的微小目标，都要用可以量化的误检增加来交换。

### 5.6 实时追踪功能的实现

检测只能告诉我们在某一帧里有哪些安全帽，但实际使用中更关心"某个人是不是一直没戴"。因此我在检测的基础上增加了多目标追踪功能（默认使用 ByteTrack，也可以选用 BoT-SORT），并针对实时性做了专门优化：

**采用的做法：** 有界异步采集队列（队列满时丢弃最旧的帧并计数）+ 隔帧检测（每 2 帧检测一次）+ 中间帧匀速外推预测 + 确认轨迹门控（连续命中 3 次才确认为真实目标）+ 类别时序投票（窗口 5 帧、多数 3 票）+ 半精度推理。

2026 年 9 月 1 日在本机实测（RTX 3060 Laptop，E4 权重，输入 960，隔帧步长 2）：

| 验收项 | 目标 | 实测 | 结果 |
|---|---|---|---|
| 完整流程平均吞吐 | ≥ 25 FPS | 25.02 FPS | ✅ |
| P95 单帧耗时 | ≤ 40 ms | 35.76 ms | ✅ |
| 队列端到端延迟 P95 | ≤ 200 ms | 36.45 ms | ✅ |
| 实际丢帧率 | < 1% | 0.0% | ✅ |
| 短轨迹（命中 ≤2 次）比例 | 明显下降 | 67.7% → 13.6% | ✅ |

> 不限制速度时的处理能力约为 51 FPS（测试视频为 876 帧）。过程中还修复了一个问题：检测帧和中间帧原本使用了不同的绘制方式，导致标签每两帧闪烁一次，统一之后的 228 帧轨迹结果完全一致。

### 5.7 检测系统的实现

为了让训练好的模型能够实际用起来，我把各个部分封装成了可以调用的程序：

- **检测服务**：基于 FastAPI 编写了一个检测接口，服务启动时自动校验模型文件（比对校验值），模型只加载一次并由并发锁保护，提供检测接口、健康检查和运行状态统计；同时提供了一个网页界面，可以直接调用电脑摄像头看到实时检测效果。
- **模型文件管理**：用一个配置文件记录每个模型对应的实验、权重文件、评估报告、类别、输入尺寸和校验值，避免用错模型。
- **自动化测试**：编写了 **263 个自动化测试**（分布在 30 个测试文件中，经实测确认），覆盖数据处理、训练、评估、推理、追踪和检测服务等各个环节，保证修改代码时不会破坏已有功能。

### 5.8 阶段性结果小结

| 项目 | 目前的结果 |
|---|---|
| 数据集 | SHWD，两个类别，训练 5,457 / 验证 607 / 测试 1,517 张 |
| 主要模型 | E4：YOLO11s，输入 960，训练 75 轮 |
| 主要模型指标 | **mAP50 = 0.9650，mAP50-95 = 0.6430**，精确率 0.9491，召回率 0.9281 |
| 结构改进最优 | E7（P2 + 轻量精炼模块）：mAP50-95 = 0.6479，类别混淆最少（10） |
| 实验数量 | 10 组对照实验，评价标准统一，判断标准事先确定 |
| 实时追踪 | 25.02 FPS，P95 单帧耗时 35.76 ms，丢帧率 0% |
| 模型导出与部署 | ONNX（opset 17）；PyTorch CUDA 37.04 FPS，ONNX Runtime CUDA 24.30 FPS，CPU 3.71 FPS |
| 代码测试 | 263 个自动化测试 |

---

## 六、存在的问题与下一步工作计划

### 6.1 目前存在的问题

1. **数据集和真实现场还有距离。** 已有研究表明 SHWD 缺少复杂环境下的极小目标，未戴帽的样本有相当一部分来自非施工场景[^shwd-limits]。目前所有的指标都是在数据集上得到的，**还不能代表系统在真实施工现场的表现**。

2. **微小目标的召回率仍然偏低，而且和整体指标形成了明显反差。** 从 E6 的阈值扫描结果可以清楚地看到这一点：选定的工作点整体未戴帽类别召回率已经达到 **0.9399**，但同一批数据中，微小目标（128 个标注框）的召回率只有 **0.6406**（检出 82 个、漏检 46 个）。也就是说，模型在整体指标看起来不错的同时，仍然会漏掉约三分之一的极小目标——而远距离、小尺度的违规目标，恰恰是安全管理最关心的。另外，E7 的安全帽类微小目标召回率只有 0.2857，但该档位总共只有 14 个标注框，样本太少，波动很大，**不足以据此下结论**。

3. **E7 还没有在实时追踪流程中验证过。** E7 的 mAP50-95 是全部实验中最高的，但计算量增加了 62%，单张推理耗时从 12.5 ms 上升到 23.5 ms。目前 E7 只在离线评估中验证过，还没有接入隔帧检测加外推预测的实时追踪流程。

4. **缺少以事件为单位的评价数据。** 目前的评估都是逐帧、逐框的指标，还没有建立违规事件的召回率和每小时误报次数的统计，因此无法回答"系统每个月可能会漏掉几起真实违规"这个问题。

5. **"达到什么程度才够用"还没有确定。** 相关的权威框架（NIST AI RMF、ISO/IEC 23894）都没有给出统一的数值标准[^nist][^iso]，需要结合具体的使用方式（仅提醒 / 人工复核 / 自动门禁）来推导。

### 6.2 下一步工作

| 序号 | 工作内容 | 预期产出 |
|---|---|---|
| **1** | **建立事件级评价方式**：整理现场或公开视频，尽可能覆盖不同摄像头、不同距离、遮挡、逆光等场景；把逐帧的检测结果聚合成持续事件（设置宽限窗口和最短持续时间） | 事件级召回率、每小时误报次数、告警延迟的统计结果 |
| **2** | **验证 E7 的实时可行性**：把 E7 的权重接入实时追踪流程（半精度 + 隔帧检测 + 中间帧外推），重新测试吞吐、延迟和丢帧率 | E7 的实时性测试结果，判断能否替代 E4 |
| **3** | **推导实用性标准**：从"允许漏报的数量"反推召回率要求（如果每月有 N 起真实违规、最多允许漏 M 起，那么最低事件召回率 = 1 − M/N） | 分级的使用建议（仅记录 / 人工复核 / 自动门禁） |
| **4** | **（视情况开展）继续尝试数据层面的改进**：如果 E7 实时化之后微小目标漏检仍然明显，再验证困难背景裁剪和上下文目标级复制 | 判断数据层面的路线是否值得继续投入 |
| **5** | **（探索性）** 如果剩余的错误主要是几个像素的定位偏差，可以尝试 RFLA 或归一化高斯瓦瑟斯坦距离的思路；如果部署时仍需要局部高分辨率，可以考虑 QueryDet 式的稀疏查询方案 | 可行性分析（非必须完成） |
| **6** | **系统完善与论文撰写** | 毕业设计论文、系统使用说明、答辩材料 |

**关于数据层面改进的后续思路。** 5.3 节那两次没有达标的实验，已经为后续提供了一条比较清晰的路径：如果继续这一方向，应该把原来那 1,043 个集中重复的整图位置，替换成**数量有上限、尺度受控、保留上下文的微小目标中心裁剪**；裁剪的来源图片扩展到全部 278 张含微小目标的训练图（而不是只用 161 张历史漏检图）；每张原图最多生成 4 个裁剪、每个目标最多作为中心 2 次。只有在裁剪这条路线确实有效之后，再考虑目标级复制的实验。

### 6.3 进度安排

| 阶段 | 时间 | 主要任务 | 阶段成果 |
|---|---|---|---|
| 开题准备 | 2026.09 | 查阅文献、整理已有工作、撰写开题报告 | 开题报告、文献综述 |
| 数据与基线 | 2026.10 | 完善数据处理流程，复现并核对基线模型的训练与评估 | 数据处理报告、基线指标 |
| 中期检查 | 2026.11 - 2026.12 | 补充小目标改进实验，验证 E7 的实时性 | 实验对比报告、E7 实时性结果 |
| 评价标准 | 2027.01 - 2027.02 | 建立事件级评价方式，推导实用性标准 | 事件级评价结果、使用建议 |
| 系统完善 | 2027.03 | 系统联调，整理演示材料和用户说明 | 可演示的系统、使用文档 |
| 论文撰写 | 2027.04 - 2027.05 | 撰写、修改、定稿 | 毕业设计论文 |
| 答辩 | 2027.05 - 2027.06 | 答辩准备与演示 | 答辩材料、最终交付物 |

### 6.4 预期成果

1. **毕业设计论文一篇**，说明数据整理方法、三阶段对照实验的设计与结论、实时化方案和实用性评价思路。
2. **一套可以运行的检测系统**，包括数据处理、模型训练、性能评估、实时追踪和检测服务等部分，并配有 263 个自动化测试。
3. **一组可参考的实验结论**，量化输入分辨率、模型容量、数据分布、检测头结构四个方面的改进收益与代价，并完整记录失败的尝试。
4. **一份实用性评价建议**，给出分级的使用标准和适用边界。

---

## 七、参考文献

> 本课题的文献调研过程与完整的来源链接记录在 [`_knowledge_base/`](_knowledge_base/) 目录下的两篇笔记中：
> `research-helmet-tiny-object-optimization-20260821.md`（小目标优化方向）与
> `research-ppe-detection-production-recall-20260826.md`（实际使用的召回率要求）。

**小目标检测方法**

[1] KISANTAL M, 等. Augmentation for small object detection[EB/OL]. arXiv:1902.07296, 2019. <https://arxiv.org/abs/1902.07296>

[2] YU X, 等. Scale match for tiny person detection[C]//Proceedings of the IEEE/CVF Winter Conference on Applications of Computer Vision (WACV). 2020. <https://openaccess.thecvf.com/content_WACV_2020/html/Yu_Scale_Match_for_Tiny_Person_Detection_WACV_2020_paper.html>

[3] WANG J, 等. A normalized Gaussian Wasserstein distance for tiny object detection[EB/OL]. arXiv:2110.13389, 2021. <https://arxiv.org/abs/2110.13389>

[4] SINGH B, DAVIS L S. An analysis of scale invariance in object detection — SNIP[C]//Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition (CVPR). 2018.

[5] SINGH B, NAJIBI M, DAVIS L S. SNIPER: Efficient multi-scale training[EB/OL]. arXiv:1805.09300, 2018. <https://arxiv.org/abs/1805.09300>

[6] YANG C, 等. QueryDet: Cascaded sparse query for accelerating high-resolution small object detection[C]//Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR). 2022.

[7] XU C, 等. RFLA: Gaussian receptive field based label assignment for tiny object detection[C]//Proceedings of the European Conference on Computer Vision (ECCV). 2022. <https://www.ecva.net/papers/eccv_2022/papers_ECCV/papers/136690518.pdf>

[8] SHRIVASTAVA A, GUPTA A, GIRSHICK R. Training region-based object detectors with online hard example mining[EB/OL]. arXiv:1604.03540, 2016. <https://arxiv.org/abs/1604.03540>

[9] LIN T Y, 等. Focal loss for dense object detection[C]//Proceedings of the IEEE International Conference on Computer Vision (ICCV). 2017. <https://openaccess.thecvf.com/content_ICCV_2017/papers/Lin_Focal_Loss_for_ICCV_2017_paper>

[10] 上下文驱动的目标检测数据增强方法[EB/OL]. 2018. <https://github.com/dvornikita/context_aug>

[11] Traffic context aware data augmentation for object detection[EB/OL]. arXiv:2205.00376, 2022. <https://arxiv.org/abs/2205.00376>

**安全帽 / 个体防护装备检测**

[12] DONG, WANG, MIAO（同济大学）, 等. Improved YOLOv10-based real-time helmet detection algorithm for complex scenarios[J]. Journal of Real-Time Image Processing, 2025, 22(6). DOI: 10.1007/s11554-025-01775-y.

[13] 改进 YOLOv5 安全帽检测研究（含 SHWD 数据集局限性论述）[J/OL]. 2024. <https://pmc.ncbi.nlm.nih.gov/articles/PMC11021566/>

[14] MRC-DETR 电力作业防护用品检测研究[J/OL]. 2025. <https://pmc.ncbi.nlm.nih.gov/articles/PMC12251792/>

[15] Jocher G, 等. Ultralytics YOLO11[EB/OL]. <https://github.com/ultralytics/ultralytics>

[16] Ultralytics 数据增强官方文档[EB/OL]. <https://github.com/ultralytics/ultralytics/blob/main/docs/en/guides/yolo-data-augmentation.md>

**风险管理与实际应用要求**

[17] NIST. AI Risk Management Framework (AI RMF): Core[EB/OL]. <https://airc.nist.gov/airmf-resources/airmf/5-sec-core/>

[18] NIST. AI RMF: Risk tolerance[EB/OL]. <https://airc.nist.gov/airmf-resources/airmf/1-sec-risk/>

[19] ISO/IEC 23894:2023, Information technology — Artificial intelligence — Guidance on risk management[S]. 2023. <https://www.iso.org/es/contents/data/standard/07/73/77304.html>

[20] Bosch Security Systems. Video content analysis: How to benchmark (White Paper)[R]. 2023. <https://cdn.commerce.boschsecurity.com/public/documents/TN_VCA_HowToBenchmar_WhitePaper_enUS_24087854475.pdf>

[21] OSHA. 1910.135 — Head protection[S]. <https://www.osha.gov/laws-regs/regulations/standardnumber/1910/1910.135>

[22] Industrial safety violation detection[EB/OL]. arXiv:2412.05531, 2024. <https://arxiv.org/abs/2412.05531>

---

## 附录 A　系统实现与运行说明

> 本附录说明程序的具体运行方式，供复现和演示使用。

### A.1 模型导出与推理性能

E4 的 `best.pt` 已经导出为固定输入尺寸、batch=1、FP32、opset 17 的 ONNX 模型（`artifacts/deployment/e4_yolo11s_960.onnx`）。导出后自动执行了 ONNX 模型检查、ONNX Runtime 加载和真实图片的试运行。

2026 年 8 月 31 日在本机实测（batch=1，输入 960，计时包含预处理、推理和后处理）：

| 推理后端 | 设备 | 平均延迟 | P50 | P95 | 帧率 |
|---|---|---|---|---|---|
| PyTorch CUDA | RTX 3060 Laptop GPU | 26.99 ms | 25.46 ms | 36.18 ms | 37.04 |
| ONNX Runtime CUDA | CUDAExecutionProvider | 41.15 ms | 41.83 ms | 62.55 ms | 24.30 |
| ONNX Runtime CPU | Intel Core i7-10870H | 269.69 ms | 258.97 ms | 343.18 ms | 3.71 |

> 测试环境：Windows 10 / i7-10870H / RTX 3060 Laptop / Python 3.14.3 / PyTorch 2.12.1+cu130 / Ultralytics 8.4.120 / ONNX 1.22.0 / ONNX Runtime 1.29.0。报告已确认 ONNX CUDA 实际使用的是 CUDAExecutionProvider。

### A.2 检测服务

检测服务默认从 `configs/model_registry.json` 中读取 E4 的 ONNX 模型，启动时校验模型文件的关联信息和校验值；如果校验不通过，服务虽然仍在运行，但就绪状态会返回 503，不会使用未经验证的模型对外提供服务。

```powershell
# 安装依赖并启动服务（默认地址 http://127.0.0.1:8000）
.\.venv\Scripts\python.exe -m pip install -e ".[service]"
.\.venv\Scripts\python.exe scripts\serve_api.py --host 127.0.0.1 --port 8000

# 检测、健康检查与状态统计
curl.exe -F "image=@D:\path\to\image.jpg" http://127.0.0.1:8000/v1/detections
curl.exe http://127.0.0.1:8000/health/ready
curl.exe http://127.0.0.1:8000/metrics
```

服务启动后，在浏览器访问 `http://127.0.0.1:8000/` 可以打开内置的监控页面，不需要另外安装前端环境。页面上可以选择本机摄像头，按设定的间隔截取画面发送到检测接口，并在画面上实时画出检测框和运行指标。

| 环境变量 | 默认值 | 用途 |
|---|---|---|
| `HELMET_MODEL_REGISTRY` | `configs/model_registry.json` | 模型配置文件 |
| `HELMET_MODEL_ID` | `e4-yolo11s-960-onnx` | 使用的模型版本 |
| `HELMET_DEVICE` | `cpu` | 推理设备 |
| `HELMET_MAX_UPLOAD_BYTES` | `10485760` | 单次上传大小上限 |
| `HELMET_MAX_IMAGE_PIXELS` | `20000000` | 解码后像素数上限 |
| `HELMET_CONFIDENCE_BASELINE` | `0.80` | 置信度基线 |
| `HELMET_CONFIDENCE_DRIFT_THRESHOLD` | `0.15` | 置信度漂移告警阈值 |

### A.3 常用命令

```powershell
# 运行全部测试
.\.venv\Scripts\python.exe -m pytest

# 数据转换与校验
.\.venv\Scripts\python.exe scripts\data\convert_shwd.py --raw D:\datasets\SHWD\VOC2028 --output D:\datasets\SHWD\processed
.\.venv\Scripts\python.exe scripts\data\validate_yolo_dataset.py --raw D:\datasets\SHWD\VOC2028 --processed D:\datasets\SHWD\processed --output D:\datasets\SHWD\audit --visualize

# 训练模型（以 M4 基线为例）
.\.venv\Scripts\python.exe scripts\train\train_baseline.py --data D:\datasets\SHWD\processed\dataset.yaml --model artifacts\models\yolo11n.pt --epochs 50 --imgsz 640 --batch 8 --workers 0 --device 0

# 图片检测 / 视频检测（默认使用 E4 权重）
.\.venv\Scripts\python.exe scripts\inference\infer_image.py --source D:\path\to\image --output artifacts\inference\images --weights artifacts\training\m45_yolo11s_e75_960_001\weights\best.pt --imgsz 960 --conf 0.25 --iou 0.70
.\.venv\Scripts\python.exe scripts\inference\infer_video.py --source D:\path\to\input.mp4 --output artifacts\inference\videos\output.mp4 --weights artifacts\training\m45_yolo11s_e75_960_001\weights\best.pt --imgsz 960 --frame-stride 1

# 实时追踪
.\.venv\Scripts\python.exe scripts\inference\track_video_realtime.py --source D:\path\to\video.mp4 --output artifacts\tracking\realtime.mp4 --weights artifacts\training\m45_yolo11s_e75_960_001\weights\best.pt --device 0 --imgsz 960 --conf 0.12 --fp16 --tracker bytetrack --frame-stride 2 --jsonl

# 导出 ONNX 并测试推理性能
.\.venv\Scripts\python.exe scripts\deploy\export_onnx.py --weights artifacts\training\m45_yolo11s_e75_960_001\weights\best.pt --output artifacts\deployment\e4_yolo11s_960.onnx --imgsz 960 --opset 17
.\.venv\Scripts\python.exe scripts\deploy\benchmark_backends.py --pt-weights artifacts\training\m45_yolo11s_e75_960_001\weights\best.pt --onnx-weights artifacts\deployment\e4_yolo11s_960.onnx --source D:\datasets\SHWD\processed\images\val\000000.jpg

# 模型文件校验
.\.venv\Scripts\python.exe scripts\deploy\verify_model_registry.py
```

### A.4 环境与依赖

- Python ≥ 3.10（本机使用 3.14.3）
- 训练与推理主要依赖：`ultralytics==8.4.120`、`opencv-python>=4.10`
- 模型导出（可选）：`onnx>=1.17`、`onnxruntime>=1.19`（CPU）或 `onnxruntime-gpu>=1.19`（CUDA，不要同时安装）
- 检测服务：`fastapi`、`uvicorn`、`python-multipart`、`onnxruntime`
- 测试：`pytest>=8`

```powershell
.\.venv\Scripts\python.exe -m pip install -e ".[train,inference,test]"   # 训练与推理
.\.venv\Scripts\python.exe -m pip install -e ".[deployment,test]"        # 模型导出
.\.venv\Scripts\python.exe -m pip install -e ".[service]"                # 检测服务
```

> ⚠️ 升级 `ultralytics` 版本前，需要先重新运行全部测试和一次冒烟训练——课题对训练过程设置了固定的随机种子并开启了确定性控制，版本变化可能影响结果的可复现性。

---

## 附录 B　项目文件说明

```text
src/helmet_safety/        核心程序（数据处理 / 训练 / 推理 / 追踪 / 服务 / 依赖检查）
scripts/                  命令行入口（数据 / 训练 / 评估 / 分析 / 推理 / 部署 / 检查）
configs/                  模型和跟踪器配置、模型文件清单
tests/                    263 个自动化测试
artifacts/                实验权重、评估报告、导出产物（见 ARTIFACTS_INDEX.md）
_knowledge_base/          文献调研笔记
```

**主要文件索引：**

| 内容 | 路径 |
|---|---|
| 主要模型权重（E4） | `artifacts/training/m45_yolo11s_e75_960_001/weights/best.pt` |
| 产物总索引与校验值 | `artifacts/ARTIFACTS_INDEX.md` |
| 实验结果统一对比 | `artifacts/evaluation/m45_yolo11s_e75_960_val_001/e0_e4_unified_comparison.md` |
| 微小目标基准报告 | `artifacts/evaluation/m45_yolo11s_e75_960_tiny_val_001/tiny_val_benchmark_report.json` |
| E5a / E5b 对比报告 | `artifacts/e5a/.../e5a_full_validation_comparison.md`、`artifacts/e5b/.../e5b_comparison.md` |
| 部署性能报告 | `artifacts/deployment/benchmark_report.md` |
| 实时追踪验收报告 | `artifacts/tracking/realtime_acceptance_report.md` |
| 模型文件清单 | `configs/model_registry.json` |
| 结构实验配置 | `configs/yolo11s-p2.yaml`、`yolo11s-p2-lfr.yaml`、`yolo11s-p2-lfr-no-eca.yaml` |

**实验产物的保存约定：** 正式实验长期保留 `best.pt`、参数、结果表和报告；`last.pt` 只在还能继续训练（含优化器状态）时保留；数据集软链接、裁剪图片、批次图等属于临时产物，实验验收后清理；E4 的路径固定不变，新的实验使用新目录，不覆盖历史结果。

---

## 附录 C　术语表

| 缩写 | 含义 |
|---|---|
| mAP50 | 交并比阈值为 0.50 时的平均精度均值 |
| mAP50-95 | 交并比阈值从 0.50 到 0.95（步长 0.05）的平均精度均值 |
| TP / FN / FP | 正确检出 / 漏检 / 误检 |
| 微小目标 / 小目标 | 按目标像素尺寸分档：不大于 10 像素 / 不大于 20 像素 |
| P2 | 高分辨率检测头层级（下采样 4 倍的特征图） |
| LFR | Lite Feature Refinement，轻量特征精炼模块 |
| ECA | Efficient Channel Attention，高效通道注意力 |
| GFLOPs | 十亿次浮点运算量，用来衡量模型的计算复杂度 |
| NMS | Non-Maximum Suppression，非极大值抑制 |
| IoU | Intersection over Union，交并比 |
| ONNX | Open Neural Network Exchange，开放神经网络交换格式 |
| SHWD | Safety Helmet Wearing Dataset，安全帽佩戴数据集 |
| PPE | Personal Protective Equipment，个体防护装备 |

---

*本报告中的实验数据均来自课题开展过程中实际运行的记录，原始报告和校验值保存在 [`artifacts/ARTIFACTS_INDEX.md`](artifacts/ARTIFACTS_INDEX.md) 中。*

[^kisantal]: 见参考文献 [1]
[^scale-match]: 见参考文献 [2]
[^nwd]: 见参考文献 [3]
[^snip]: 见参考文献 [4]
[^sniper]: 见参考文献 [5]
[^querydet]: 见参考文献 [6]
[^rfla]: 见参考文献 [7]
[^ohem]: 见参考文献 [8]
[^focal]: 见参考文献 [9]
[^context-aug]: 见参考文献 [10]
[^traffic-context]: 见参考文献 [11]
[^yolov10]: 见参考文献 [12]
[^shwd-limits]: 见参考文献 [13]
[^mrcdetr]: 见参考文献 [14]
[^ultralytics]: 见参考文献 [15]、[16]
[^nist]: 见参考文献 [17]、[18]
[^iso]: 见参考文献 [19]
[^bosch]: 见参考文献 [20]
[^osha]: 见参考文献 [21]
