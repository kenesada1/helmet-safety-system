# 任务清单

## 当前任务

（暂无待处理任务）

## 已完成

### 2026-09-01 — README 补充生产化章节并推送远端

**问题描述：** 项目已由其他 agent 补齐生产化（FastAPI 服务/监控/模型 registry/CI/DVC/Docker/浏览器监控页），README 的 6.1/6.2 章节虽已更新，但存在遗漏与不一致：核心速览表、系统架构图、源码模块表未反映新增能力；里程碑缺少生产化入口；Docker 用法缺命令；测试数在任务清单（262）与 README（263）间冲突。

**解决方案：**
- 核实测试数：`pytest --collect-only` 实测 **263 项**，修正任务清单中的 262。
- 速览表补充服务化、模型治理、生产门禁行，并补 ORT CUDA 指标。
- 系统架构图新增「服务与生产门禁」层（FastAPI/registry/CI/DVC）。
- 源码模块表补充 `service/`、`quality/` 两行；里程碑新增 M7 生产化入口。
- 补充 Docker 构建/运行命令与 GPU 部署说明。
- `.gitignore` 追加 `tmp/` 与 `/yolo26n.pt`，避免临时产物入库。
- 提交 `6a8e255`（127 文件，+19598/-180）并推送 `origin/main`，工作区干净。

**新增/修改文件：** 修改 `README.md`、`任务清单.md`、`.gitignore`；推送全部已暂存变更到远端。

### 2026-09-01 — 浏览器摄像头实时监控页面

**问题描述：** FastAPI 已提供图片检测接口，但没有可见监控页面；用户无法在浏览器选择本机摄像头、启动/停止监控或直接观察模型检测框和推理指标。

**解决方案：** 新增由 FastAPI 8000 端口同源提供的单页监控台：
- 浏览器 `getUserMedia` 摄像头选择、权限错误说明和资源释放；无需单独前端端口或构建服务。
- 非重叠定时截图请求 `/v1/detections`，Canvas 实时绘制 `helmet` 绿色框与 `no_helmet` 红色框。
- 可调推理间隔和显示阈值，展示推理 FPS、模型延迟、当前类别计数、最近检测事件及服务请求/错误量。
- 桌面采用大画面 + 设置栏，移动端自动单列；提供清晰的服务、摄像头、空闲、运行和错误状态。
- FastAPI 根路径和 `/assets` 提供页面资源，Docker 构建会随 `src` 一并包含。

**测试与验证：** 按 TDD 新增监控页同源服务测试，先确认根路径 404 再实现转绿；JavaScript 语法检查通过。内置浏览器在 1440×900 和 390×844 验证无横向溢出、无控制台警告/错误，区间与阈值控件状态更新正确。真实 E4 ONNX HTTP 推理返回 200、1 个检测框，页面指标随后读取到请求成功 1 / 错误 0。摄像头实体权限保留给用户在浏览器中确认。

**新增/修改文件：** 新增 `src/helmet_safety/service/frontend/{index.html,styles.css,app.js}`；修改 `service/api.py`、`tests/test_service_api.py`、`README.md`、`AGENT_HANDOFF.md`。

### 2026-09-01 — 补齐生产化缺口（CI / HTTP 服务 / 监控 / 模型治理 / 跨机编排）

**问题描述：** 项目实验链路成熟，但交接文档指出缺少 CI 门禁、HTTP 推理服务、持续监控、模型与报告的版本关联、准确项目元数据及跨机声明式编排，导致当前成果难以稳定进入生产环境。

**解决方案：**
- 新增 FastAPI E4 ONNX 推理服务：模型单次加载、并发互斥、上传字节/像素限制、liveness/readiness 与稳定 Pydantic 响应；提供脚本和 Docker 入口。
- 新增线程安全监控：请求成功/失败、P50/P95 延迟、分类检测量、置信度滚动漂移告警和 GPU 显存，同时输出 Prometheus 文本与 JSON。
- 新增版本化模型 registry，把 E4 的实验、阶段、PT/ONNX 权重、评估/导出报告、类别、输入尺寸和 SHA256 关联；服务启动与独立 CLI 均执行 fail-closed 验真。
- 新增 GitHub Actions 门禁：依赖策略检查、全量测试、无需 SHWD/预训练下载的一轮真实 YOLO CPU 训练及证据上传；新增直接依赖锁文件。
- 新增 `dvc.yaml` / `params.yaml`，声明数据审计、转换、独立验证及训练冒烟，消除编排层的机器绝对路径。
- 更新 `pyproject.toml` 定位、service/pipeline 依赖和 FastAPI 入口；同步更新 README 与交接状态。

**测试与验证：** 严格按 TDD 新增 15 项测试（服务 4、监控 2、registry 3、CI 门禁及安全清理 5、ONNX 任务声明回归 1），均先因功能缺失失败再实现转绿；模型 registry 已对本机 E4 ONNX 的 SHA256 完整验真；自包含的一轮 CPU 训练冒烟已实际通过并生成完整训练证据。全量测试最终为 263 项（pytest --collect-only 实测确认）。

**新增/修改文件：** 新增 `src/helmet_safety/service/`、`src/helmet_safety/quality/`、`training/ci_smoke.py`、`scripts/ci/`、`scripts/serve_api.py`、模型 registry、GitHub Actions、DVC/参数文件、锁文件与 Dockerfile；修改 `pyproject.toml`、`README.md`、`AGENT_HANDOFF.md`。

### 2026-09-01 — 交付 Agent Handler 交接文档

**问题描述：** 前一轮 README 重写与成熟度分析的结论需要沉淀为可交付给其他 agent 的上下文文件，避免下一个 agent 重新做一遍分析。

**解决方案：** 新建 `AGENT_HANDOFF.md`，内容：
- 项目定位与当前状态总览（数据/主模型/指标/实时性/部署/测试）
- 核心结论移交：成熟度评分（实验9/工程7/生产5）、十轮实验通/不通清单、阈值结论、E4 vs E7 生产决策
- **评估口径差异警示**（E5 系列以 E4 候选 C=conf0.20/iou0.50 为锚点，E6-E8 系列以标准配置 conf0.25/iou0.70 为锚点）
- 工作守则（实验纪律/数据纪律/训练复现纪律/代码质量纪律）
- 常用操作脚本索引、已知缺口待办清单（CI/服务化/权重版本/监控）、关键文件索引

**新增/修改文件：** 新增 `AGENT_HANDOFF.md`；修改 `任务清单.md`。

### 2026-09-01 — README 全面重写 + 项目成熟度分析

**问题描述：** 原 README 定位为「SHWD 数据审计与 VOC→YOLO 转换工具」使用手册，未体现项目整体的检测系统定位、算法思想与十轮模型微调实验的对比结果；模型优化路径（M4/E1-E8）的指标散落在各 artifacts 报告中，缺少统一视角。

**解决方案：** 将 README 重写为项目级总览，核心变更：
- 新增「算法思想」章节：数据可信优先、诊断驱动受控实验、小目标四层杠杆、精度-算力-实时三角权衡
- 新增「模型优化：十轮受控实验全记录」章节，按三阶段（主干选型 M4→E4 / 数据级 E5a/E5b / 结构级 E6/E7/E8）绘制对比表，并标注各实验评估口径差异（E5 系列以 E4 候选 C=conf0.20/iou0.50 为锚点，E6-E8 以标准配置 conf0.25/iou0.70 为锚点）
- 补充阈值校准（conf/NMS IoU 单变量扫描）、部署基准、系统架构图与常用命令
- 输出成熟度分析结论：科研/实验型维度已相当成熟完善；生产化维度（CI、服务化、监控、权重托管）仍有明确缺口

**新增/修改文件：** 修改 `README.md`、`任务清单.md`。

### 2026-09-01 — M6 目标追踪实时性优化

**问题描述：** 以视频推理为基准的目标追踪未达实时性要求。基线（stride=1，E4 YOLO11s 960，ByteTrack，同步串行流水线）在真实视频（1280×720 / 25 FPS / 876 帧）上：完整链路吞吐仅 14.41 FPS，平均完整帧 66.7 ms，hits≤2 短轨迹占比 67.7%，且无队列/丢帧治理、无类别稳定性处理。

**解决方案：** 新增实时追踪流水线 `src/helmet_safety/tracking/realtime.py` 与 CLI `scripts/inference/track_video_realtime.py`，核心手段：
- 有界异步采集队列（满时丢最旧帧并计数；默认按源 FPS 节流模拟实时摄像头，丢帧率 0%）
- 隔帧检测（stride=2）+ 中间帧匀速外推预测，保持轨迹视觉连续
- confirmed track 门控（hits≥3）+ 类别时序投票（窗口 5 / 多数 3）
- FP16 推理（E4 best.pt 权重不变）+ max_det=80 + conf=0.12 + avc1 编码 + 检测帧 lite 绘制 + GPU 预热 8 次

**验收结果（real_c505_realtime.json / realtime_acceptance_report.md）：**
- 吞吐 25.02 FPS（≥25 达标）；不节流处理能力约 51 FPS
- P95 完整帧 35.76 ms（≤40 达标）
- 队列端到端延迟 P95 36.45 ms（≤200 达标）
- 丢帧率 0.0%（<1% 达标）
- hits≤2 短轨迹 67.7% → 13.6%（检测口径）/ 0.0%（观测口径）
- class_switches=47、recovered=29、id_switch_estimate=0 独立报告

**新增/修改文件：** 新增 `tracking/realtime.py`、`scripts/inference/track_video_realtime.py`、`tests/test_realtime_tracking.py`（14 测试）；修改 `tracking/drawing.py`（lite 绘制）、`inference/opencv.py`（fp16）、`inference/videos.py`（编码器顺序）、`README.md`。全套测试 247 通过。

**后续修复（2026-09-01）：** 生产 MP4 标签闪动问题——根因是检测帧用 lite 绘制、中间帧用完整绘制，两种风格每 2 帧交替导致标签/面板时有时无。修复：检测帧与中间帧统一 lite 风格（都画框 + 描边 ID 标签 + 单行状态，无 lost 框与完整面板）；中间帧只对 active 轨迹外推（检测帧未输出的轨迹标记为非 active），最终 228/228 帧对轨迹集完全一致，无闪烁。P95 完整帧 35.6 ms 仍达标。
